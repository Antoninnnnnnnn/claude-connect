[Unit]
Description=Ecole Directe API
After=network.target

[Service]
WorkingDirectory=/path/to/claude-connect/ecoledirecte
ExecStart=/path/to/claude-connect/ecoledirecte/venv/bin/uvicorn main:app --host 0.0.0.0 --port 8093
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
