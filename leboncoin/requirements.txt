# Pinned to the versions this service is running and tested against.
fastapi==0.136.3
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
curl-cffi==0.15.0
httpx==0.28.1
# 1.1.3 is the version _sanitize_payload in app/lbc_client.py works around.
# Newer releases may fix those bugs: re-check that helper before bumping.
lbc==1.1.3
