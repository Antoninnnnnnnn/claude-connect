[pytest]
# Root config covers the opt-in live canaries only; each service has its own
# pytest.ini because they run in separate virtualenvs.
testpaths = tests_live
markers =
    live: hits real upstream services (opt-in, run with -m live)
